on: [push]
jobs:
  sshd_cloudflared:
    runs-on: ubuntu-latest
    name: "Sshd Cloudflared"
    steps:
      - use: actions/checkout@v3
      - id: composite
        uses: amstests/sshd-cloudflared@v1
